<% content_for :title, t("views.admin.persons.edit") %>

<%= render "admin/nav" %>

<h1 class="text-2xl font-bold text-slate-900 dark:text-slate-100 mb-6">
  <%= t("views.admin.persons.edit") %>: <%= @person.full_name %>
</h1>

<%= render "form", person: @person %>
