<div class="flex flex-col-reverse sm:flex-row sm:items-center gap-3 pt-6 border-t border-slate-200 dark:border-slate-700 print:hidden">
  <%= admin_submit(form, label: local_assigns[:submit_label]) %>
  <%= admin_cancel_link(cancel_path) %>
</div>
